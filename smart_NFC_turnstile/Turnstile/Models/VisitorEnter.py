class VisitorEnter:
	def __init__(self, Id, AuthenId, Temp, image, Time, Permission):
		self.Id = Id
		self.AuthenId = AuthenId
		self.image = image
		self.Temp = Temp
		self.Time = Time
		self.Permission = Permission

	def image_save()
	# save the image to the path 

		